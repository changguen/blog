- [[Dockerfile]] 등에서 ENV로 주입하거나, [[도커 컴포즈|docker-compose]]에서 `env_file`로 주입하는 것은 모두 노출될 수 있다.
	- 컨테이너 inspect (둘다 노출)
	- 파일 시스템에 평문으로 노출 (`.env`파일)
- 따라서 프로덕션 환경에서는 더 안전한 저장소를 사용해야 한다.
## Docker secrets
