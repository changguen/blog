- **메모리 사이에서 데이터를 복사하는 시간을 최적화하는 것이다.**
- 일반적인 전송방식
	- 디스크 -> 커널 버퍼 (커널 공간의 페이지 캐시)
	- 커널 버퍼 -> 유저 버퍼 (카프카 애플리케이션)
	- 유저 버퍼 -> 소켓 버퍼
	- 소켓 버퍼 -> NIC 버퍼 (네트워크 카드)
- **제로 카피 : 리눅스의 `sendfile()` 시스템 콜을 통해 과정을 단순화한다.**
  : 디스크 -> 커널 -> NIC
- 장점
	- CPU 부하 감소
	- 메모리 대역폭 감소
	- 처리량 증가