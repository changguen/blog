- **데이터를 관리하는 최소 작업 단위**이다.
  디스크에서 메모리로 데이터를 읽어오거나 쓸 때, 페이지 단위로 처리한다.
- 디스크를 읽는 작업은 비용이 매우 크므로, 다음에 읽힐 확률이 높은 데이터까지 같이 가져오는 것이다.
- [[B+Tree 인덱스]]에서도 각 노드가 페이지이다. 즉, 인덱스를 탐색하는 중에 메모리에 없는 노드가 존재한다면 페이지 단위로 읽게 되는 것이다. (루트/리프/중간 노드 모두)
- [[RealMySQL|MySQL]]에서는 이 페이지 단위로 모든 작업이 수행된다.