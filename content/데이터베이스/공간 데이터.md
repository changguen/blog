![[Pasted image 20251113220550.png]]
- 공간 데이터는 말 그대로 존재할 수 있는 모든 기하들을 의미하는 데이터이다.
- 다음과 같은 종류가 존재한다.
	- `Point` : 점
	- `MulitPoint` : 여러 점
	- `LineString` : 선(좌표 리스트)
	- `MultiLineString` : 여러 선
	- `Polygon` : 면
	- `MultiPolygon` : 여러 면
	- `GeometryCollection` : 위 공간 데이터들의 묶음 (종류 무관)